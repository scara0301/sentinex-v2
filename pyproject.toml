[tool.uv.workspace]
members = [
  "apps/api",
  "apps/worker",
  "apps/proxy",
  "apps/mocks",
  "packages/core",
]

[tool.uv]
dev-dependencies = [
  "pytest>=8.0",
  "pytest-asyncio>=0.23",
  "testcontainers[postgres,redis]>=4.0",
  "httpx>=0.27",
  "mypy>=1.10",
  "ruff>=0.4",
]
